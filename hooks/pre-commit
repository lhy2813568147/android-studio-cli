#!/bin/bash
# Git pre-commit hook for Android Studio CLI
# 将此文件复制到 .git/hooks/pre-commit

echo "运行代码检查..."

# 运行lint检查
python -m cli_anything.android_studio gradle lint --variant debug

if [ $? -ne 0 ]; then
    echo "Lint检查失败，请修复后再提交"
    exit 1
fi

echo "代码检查通过"